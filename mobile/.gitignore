# Xcode build artifacts
DerivedData/
*.xcuserstate
xcuserdata/

# macOS
.DS_Store

# Swift Package Manager (added via Xcode, not version-controlled)
.build/
*.resolved

# project.yml is source of truth — xcodeproj is committed so CI/Xcode don't need xcodegen
